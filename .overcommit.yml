CommitMsg:
  GerritChangeId:
    enabled: true

PreCommit:
  Rubocop:
    command: ['bundle', 'exec', 'rubocop']
  TravisLint:
    command: ['bundle', 'exec', 'travis']
    flags: ['--skip-version-check']
